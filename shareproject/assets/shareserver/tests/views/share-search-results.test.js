define([ 'share_results', 'share_router', 'mocha', 'chai' ], function(ShareResults, ShareRouter, mocha, chai) {

    var should = chai.should();
	describe("ShareResults", function() {
        it("should have some tests", function() {
            should.fail();
        });
	});
	
});